# Contributing

Keep changes generic, offline, bounded, and backed by tests. Add no private names, source paths, URLs, logs, or secrets. Run the unit suite, public-boundary check, functional probe, and offline wheel build before opening a pull request.

