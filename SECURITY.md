# Security policy

Report vulnerabilities through GitHub private vulnerability reporting when
available. Do not publish credentials or exploit details in a public issue.

Agent Inbox coordinates trusted local processes; it is not an authentication system.
Protect the SQLite file and lease tokens using operating-system permissions. Payload,
reason, event detail, and evidence fields are persisted and must never contain secrets.
Use an external identity layer when agent identity must be cryptographically proven.

