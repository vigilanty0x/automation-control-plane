# Safety

Use reserved or synthetic identities and artifact URIs in public examples. Never include credentials, proprietary logs, client details, internal topology, or private repository paths.

Path scopes must be relative and may not contain `..`. Limits are non-negative integers. A high blocker or escalation prevents `done`, as does missing evidence or any unmet acceptance criterion.

