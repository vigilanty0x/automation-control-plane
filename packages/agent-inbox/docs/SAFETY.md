# Public safety boundary

Fixtures use invented agent names, repository labels, hashes, and mission payloads.
The project contains no accounts, credentials, private repository references,
production telemetry, or network calls. The CLI accesses only the database and JSON
paths explicitly supplied by the caller.

