# Safety

TaskGraph schedules metadata; it never executes arbitrary task content. Path scopes must be relative and non-overlapping. Leases, retries, task count, evidence lists, and input size are bounded. Use synthetic identities and artifacts in public examples.

